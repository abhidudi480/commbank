This pull request adds unit tests for the GetGoalsForUser route.

The test verifies that:
- The API returns goals for a specific user.
- The response status is correct.
- The returned data matches the expected goals.

This improves the test coverage of the GoalsController.
